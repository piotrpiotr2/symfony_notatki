{% extends 'base.html.twig' %}

{% block title %}
    {{ todoList.title }}
{% endblock %}

{% block body %}
    <a href="{{ url('todolist_index') }}" title="{{ 'action.back'|trans }}">
        {{ 'action.back'|trans }}
    </a>

    <h1>{{ todoList.title }}</h1>

{% endblock %}